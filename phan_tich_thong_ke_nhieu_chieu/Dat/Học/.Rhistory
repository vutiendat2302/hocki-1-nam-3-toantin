HotellingsT2(x, mu = c(0,0,0,0,0,0))
HotellingsT2(x_2004, x_2005, paired = F)
HotellingsT2(x_2004, x_2005)
HotellingsT2(x_2004, x_2005, paired = F)
HotellingsT2(x_2004, x_2005, paired = T)
HotellingsT2(x_2004, x_2005, paired = F)
HotellingsT2(x_2004, x_2005, paired = F)
install.packages("reshape2")
# install.packages("reshape2")
library(reshape2)
install.packages("ggplot2")
library(ICSNP)
#4. Lợi nhuận trung bình tại ngày đang xét và các ngày trước đó có thực sự khác (0,0)′không?
# Dùng hàm HotellingT2 kiểm định xem GTTB của
# (Today, Lag1, Lag2, Lag3, Lag4, Lag5 có thực sự khác (0,0,0,0,0,0) không ? - 1 Mẫu)
install.packages("ICSNP")
#4. Lợi nhuận trung bình tại ngày đang xét và các ngày trước đó có thực sự khác (0,0)′không?
# Dùng hàm HotellingT2 kiểm định xem GTTB của
# (Today, Lag1, Lag2, Lag3, Lag4, Lag5 có thực sự khác (0,0,0,0,0,0) không ? - 1 Mẫu)
# install.packages("ICSNP")
library(ICSNP)
inverst = read.table("D:\\hocki-1-nam-3-toantin\\hocki-1-nam-3-toantin\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
inverst = read.table("D:\\hoc_ki_1_nam_hoc_2025_2026\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
head(inverst)
rownames(inverst)  = inverst$Manager
rownames
inverst = read.table("D:\\hoc_ki_1_nam_hoc_2025_2026\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
head(inverst)
rownames(inverst)  = inverst$Manager
inverst[, -1]
inverst = read.table("D:\\hoc_ki_1_nam_hoc_2025_2026\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
head(inverst)
rownames(inverst)  = inverst$Manager
inverst[, -1]
colnames(inverst) = c("S.US", "S.U.US","S.Dev.","B.US","B.Non.US","B.Dev","A.Na","A.C")
head(inverst)
inverst = read.table("D:\\hoc_ki_1_nam_hoc_2025_2026\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
head(inverst)
rownames(inverst)  = inverst$Manager # Kiểu như gán hay thay STT bởi các giá trị trong cột Manager rồi mình sẽ xóa cột manager
inverst[, -1] # bỏ đi cột manager
colnames(inverst) = c("S.US", "S.U.US","S.Dev.","B.US","B.Non.US","B.Dev","A.Na","A.C") # Đổi, gán tên cho các cột trong bảng inverst
head(inverst)
inverst = read.table("D:\\hoc_ki_1_nam_hoc_2025_2026\\phan_tich_thong_ke_nhieu_chieu\\Dat\\Học\\investment.txt", header = T)
head(inverst)
rownames(inverst)  = inverst$Manager # Kiểu như gán hay thay STT bởi các giá trị trong cột Manager rồi mình sẽ xóa cột manager
inverst = inverst[, -1] # bỏ đi cột manager
colnames(inverst) = c("S.US", "S.U.US","S.Dev.","B.US","B.Non.US","B.Dev","A.Na","A.C") # Đổi, gán tên cho các cột trong bảng inverst
head(inverst)
#a. Xét trên tập dữ liệu:
pc = princomp(invest) #== princomp(invest, cor = FALSE)
#a. Xét trên tập dữ liệu:
pc = princomp(inverst) #== princomp(invest, cor = FALSE)
# Xem xét với cov = 1/n * sum((y - y_)^2)
#computes eigenvalues and eigenvectors and presents these in a suitable format
s = summary(pc)
s #Sd = standard deviations = sqrt(λ1) >=...>= sqrt(λ8) với λi là gtr của ma trận hiệp phương sai
s$sdev #SD(Yi) gần đúng == sqrt(λi)
#Tính sqrt(λi)
sqrt(eigen(cov(inverst))$values) #sqrt(λi)
#a. Xét trên tappj dữ liệu:
pc = princomp(inverst) #== princomp(inverst, cor = false)
# xem xét với cov = 1/n * sum((y - y_)^2)
# computes eigenvalues and eigenvectors and presents thế in a suitable format
s = summary(pc)
knitr::opts_chunk$set(echo = TRUE)
s$sdev #SD(Yi) gần đúng == sqrt(λi)
# b. Xét trên ma trận hiệp phương sai của tập dữ liệu: cov=1/(n-1)*sum(y-y_h)
pcacov = princomp(covmat = cov(invest))
# b. Xét trên ma trận hiệp phương sai của tập dữ liệu: cov=1/(n-1)*sum(y-y_h)
pcacov = princomp(covmat = cov(inverst))
summary(pcacov)
help("princomp")
#2. Thành phần chính đầu tiên giải thích hơn 80% thông tin bộ dữ liệu ban đầu:
# Tải trọng: giữa data và cov(data) xuất ra giống nhau
pc$loadings
# Tự truyền vào dữ liệu hiệp phương sai -> covmat
summary(pcacov, loadings = T)
#2. Thành phần chính đầu tiên giải thích hơn 80% thông tin bộ dữ liệu ban đầu:
# Tải trọng: giữa data và cov(data) xuất ra giống nhau
pc$loadings
pcacov$loadings #factor loadingsL ma trận tải trọng: PC_i = Y_i = e_i' X
eigen(cov(invest))$vectors #e_i
eigen(cov(inverst))$vectors #e_i
summary(pcacov, loadings = TRUE)
# Tự truyền vào dữ liệu hiệp phương sai -> covmat
summary(pcacov)
eigen(cov(inverst))$vectors #e_i
summary(pcacov, loadings = TRUE)
pc <- princomp(invest)
pc <- princomp(inverst)
biplot(pc, col = c(2,4),
cex = c(.5,1.25),#màu và độ lớn của quan sát và biên
xlim = c(-.45, .45),
xlab = "First principal component",
ylab = "Second principal component",
main = "Biplot for investment allocations")
max(invest$S.US)
max(inverst$S.US)
invest["LeggMason",]$S.US
max(inverst$S.US)
inverst["LeggMason",]$S.US
inverst["HarrisPrivateBank",]$S.US
inverst["HighmountCapital",]$B.US
# bipblot: Vẽ cho data (loadings như cov) hoặc cor
# Biểu đồ hiển thị cùng lúc các điểm dữ liệu và các biến gốc trong không gian PCA
pc <- princomp(inverst)
biplot(pc, col = c(2,4),
cex = c(.5,1.25),#màu và độ lớn của quan sát và biên
xlim = c(-.45, .45),
xlab = "First principal component",
ylab = "Second principal component",
main = "Biplot for investment allocations")
max(inverst$S.US)
inverst["LeggMason",]$S.US
inverst["HarrisPrivateBank",]$S.US
inverst["HighmountCapital",]$B.US
#c Phân tích trên ma trận tương quan
princomp(covmat = cor(invest))
#c Phân tích trên ma trận tương quan
princomp(covmat = cor(inverst))
princomp(inverst, cor=TRUE)
summary(princomp(covmat = cor(inverst)),
loadings = TRUE)
summary(princomp(inverst, cor = TRUE),
loadings = TRUE)
biplot(princomp(inverst, cor = TRUE),
col = c(2,4),
cex = c(.5,1.25),
xlim = c(-.45,.45),
xlab = "First principal component",
ylab = "Second principal component",
main = "Biplot for investment allocations")
#screeplot
screeplot(pc, col = "red", pch = 16, type = "lines", cex = 2, lwd = 2)
screeplot(pcacov, col = "red", pch = 16, type = "lines", cex = 2, lwd = 2)
summary(princomp(covmat = cor(inverst)),
loadings = TRUE)
summary(princomp(inverst, cor = TRUE),
loadings = TRUE)
biplot(princomp(inverst, cor = TRUE),
col = c(2,4),
cex = c(.5,1.25),
xlim = c(-.45,.45),
xlab = "First principal component",
ylab = "Second principal component",
main = "Biplot for investment allocations")
data(USJudgeRatings)
View(USJudgeRatings)
#  Thực hiện phân tích thành phần chính trên tập dữ liệu, hãy cho biết nếu dùng 5 thành phần chính đầu thì tỉ lê dữ liệu là bao nhiêu
pca = princomp(USJudgeRatings)
summary(pca)
loadings_cov = pca$rotation[, 1:5]
loadings_cov
print(loadings_cov[c("CONT", "CFMG"), ])
loadings_cov = pca$rotation[, 1:5]
print(loadings_cov[c("CONT", "CFMG"), ])
loadings_cov = pca$loadings[, 1:5]
loadings_cov
loadings_cov[c("CONT", "CFMG"), ]
# Đóng góp = Bình phương hệ số tải
contribution = loadings_cov[c("CONT", "CFMG"), ] ^ 2
# Đóng góp = Bình phương hệ số tải
contribution = loadings_cov[c("CONT", "CFMG"), ] ^ 2
contri = contribution * 100
contri
# Biểu đồ Scree plot cho ma trận HIỆP PHƯƠNG SAI
screeplot(pca, type = "lines", main = "Scree Plot (Sử dụng Ma trận Hiệp phương sai)")
# Thực hiện PCA với ma trận tương quan
summary(pca)
plot(pca_cor,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19,
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai (%)",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính\n(Sử dụng ma trận tương quan)")
pca_cor <- princomp(USJudgeRatings, cor = TRUE)
# Thực hiện PCA với ma trận tương quan
summary(pca_cor)
plot(pca,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19,
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai (%)",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính\n(Sử dụng ma trận tương quan)")
plot(pca,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19)
plot(pca,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19,
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai (%)",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính\n(Sử dụng ma trận tương quan)")
plot(pca,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19,
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai (%)")
plot(pca,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19)
# Vẽ thêm biểu đồ tích lũy tỷ lệ phương sai
cum_var <- cumsum(pca_cor$sdev^2 / sum(pca_cor$sdev^2)) * 100
lines(1:length(cum_var), cum_var, type = "b", col = "red", pch = 17)
legend("bottomright", legend = c("Từng thành phần", "Tích lũy"),
col = c("blue", "red"), pch = c(19, 17))
pcs <- (pca_cor$sdev^2) / sum(pca_cor$sdev^2) * 100
# Vẽ biểu đồ cột (barplot)
barplot(pcs,
names.arg = paste0("PC", 1:length(pcs)),
main = "Tỷ lệ phương sai của các thành phần chính",
ylab = "Tỷ lệ phương sai (%)",
col = "lightblue")
plot(pca_cor,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19,
main = "Tỷ lệ phương sai của các thành phần chính",
ylab = "Tỷ lệ phương sai (%)")
plot(pca_cor,
type = "b",                   # dạng đường nối giữa các điểm
col = "blue",
pch = 19)
barplot(pcs,
names.arg = paste0("PC", 1:length(pcs)),
main = "Tỷ lệ phương sai của các thành phần chính",
ylab = "Tỷ lệ phương sai (%)",
col = "lightblue")
lines(x = 1:length(pcs),
y = cum_var,
type = "b",
col = "red",
pch = 19)
text(1:length(pcs), cum_var + 2,
labels = paste0(round(cum_var, 1), "%"),
col = "red", cex = 0.8)
screeplot(pca_cor, type = "lines", main = "Scree Plot (Sử dụng Ma trận Tương quan)")
# Vẽ biểu đồ hai thành phần trong trường hợp sử dụng ma trận tương quan.
biplot(pca_cor, main = "Biplot (Sử dụng Ma trận Tương quan)", cor = T)
# Vẽ biểu đồ hai thành phần trong trường hợp sử dụng ma trận tương quan.
biplot(pca_cor, main = "Biplot (Sử dụng Ma trận Tương quan)")
biplot(pca_cor,
cex = c(0.7, 1.1),
col = c("darkgray", "blue"),
xlab = "Thành phần chính 1 (PC1)",
ylab = "Thành phần chính 2 (PC2)",
main = "Biểu đồ hai thành phần chính (dựa trên ma trận tương quan)")
#5. Tiến hành phân tích nhân tố cho bộ dữ liệu trên với 4 nhân tố. Hãy cho biết tỉ lệ tổng phương sai của 4 nhân tố
# Phân tích nhân tố (Factor Analysis) với 4 nhân tố
fa4 <- factanal(USJudgeRatings, factors = 4, rotation = "varimax")
# Xem kết quả
print(fa4, digits = 3, cutoff = 0.3)
# Tính tổng phương sai được giải thích bởi 4 nhân tố
total_var_4 <- sum(fa4$PVAL)
total_var_4
# Bài 2:
# Bộdữliệu wdbc {kdevine}trongRđượcthuthậptừBệnhviệnĐạihọcWisconsin,Madison, và được sử dụng để hỗ trợ trong việc chẩn đoán ung thư vú thông qua các đặc điểm của tế bào. Bộ dữ liệu gồm 569 quan sát và 31 biến, cụ thể các đặc trưng: Radius (Độdài trung bình từ tâm đến các điểm trên chu vi), Texture (Độ lệch chuẩn của các giá trịxám), Perimeter (Chu vi của tế bào), Area (Diện tích của tế bào), Smoothness (Biến thiêncục bộ trong độ dài bán kính), Compactness (Tính toán từ chu vi và diện tích), Concavity(Mức độ nghiêm trọng của các phần lõm trên đường viền), Concave Points (Số lượng phầnlõm trên đường viền), Symmetry (Độ đối xứng của tế bào), Fractal Dimension (Đánh giá độphức tạp của hình dạng tế bào)
install.packages(kdevine)
# Bài 2:
# Bộdữliệu wdbc {kdevine}trongRđượcthuthậptừBệnhviệnĐạihọcWisconsin,Madison, và được sử dụng để hỗ trợ trong việc chẩn đoán ung thư vú thông qua các đặc điểm của tế bào. Bộ dữ liệu gồm 569 quan sát và 31 biến, cụ thể các đặc trưng: Radius (Độdài trung bình từ tâm đến các điểm trên chu vi), Texture (Độ lệch chuẩn của các giá trịxám), Perimeter (Chu vi của tế bào), Area (Diện tích của tế bào), Smoothness (Biến thiêncục bộ trong độ dài bán kính), Compactness (Tính toán từ chu vi và diện tích), Concavity(Mức độ nghiêm trọng của các phần lõm trên đường viền), Concave Points (Số lượng phầnlõm trên đường viền), Symmetry (Độ đối xứng của tế bào), Fractal Dimension (Đánh giá độphức tạp của hình dạng tế bào)
library(kdevine)
data(wdbc)
# Bài 2:
# Bộdữliệu wdbc {kdevine}trongRđượcthuthậptừBệnhviệnĐạihọcWisconsin,Madison, và được sử dụng để hỗ trợ trong việc chẩn đoán ung thư vú thông qua các đặc điểm của tế bào. Bộ dữ liệu gồm 569 quan sát và 31 biến, cụ thể các đặc trưng: Radius (Độdài trung bình từ tâm đến các điểm trên chu vi), Texture (Độ lệch chuẩn của các giá trịxám), Perimeter (Chu vi của tế bào), Area (Diện tích của tế bào), Smoothness (Biến thiêncục bộ trong độ dài bán kính), Compactness (Tính toán từ chu vi và diện tích), Concavity(Mức độ nghiêm trọng của các phần lõm trên đường viền), Concave Points (Số lượng phầnlõm trên đường viền), Symmetry (Độ đối xứng của tế bào), Fractal Dimension (Đánh giá độphức tạp của hình dạng tế bào)
install.packages("kdevine")
library(kdevine)
data(wdbc)
# Tiến hành tiền xử lý dữ liệu, bao gồm việc loại bỏ các cột không cần thiết và chuyển đổi các biến phân loại thành biến số.
View(wdbc)
# loại bỏ các cột không cần thiết (id, diagnosis)
# Giữ lại các cột đặc trung
data = wdbc[, -c(1, 2)]
head(data)
rownames(data) = wdbc$diagnosis
diag = as.numeric(as.factor(wdbc$diagnosis))
diag
data = data.frame(diag, data)
head(data)
data = cbind(diag, data)
data
data(wdbc)
# loại bỏ các cột không cần thiết (id, diagnosis)
# Giữ lại các cột đặc trung
data = wdbc[, -c(1, 2)]
diag = as.numeric(as.factor(wdbc$diagnosis))
data = cbind(diag, data)
data
# 2. Chuẩn hóa dữ liệu để đảm bảo rằng tất cả các biến có cùng quy mô:
data_scale = scale(data)
data_scale
# loại bỏ các cột không cần thiết (id, diagnosis)
# Giữ lại các cột đặc trung
data = wdbc[, -c(1, 2)]
diag = as.numeric(as.factor(wdbc$diagnosis))
# 2. Chuẩn hóa dữ liệu để đảm bảo rằng tất cả các biến có cùng quy mô:
data_scale = scale(data)
data_scale
# 3.  Tính toán ma trận hiệp phương sai và tìm giá trị riêng, véc-tơ riêng.
cov_matrix = cov(data_scale)
eig = eigen(cov_matrix)
eig$values
eig$vectors
pca_wdbc <- prcomp(wdbc_scaled, cor = T)
pca_wdbc <- prcomp(data_scale, cor = T)
summary(pca_wdbc)
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
plot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)",
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai")
# Biểu đồ tỷ lệ phương sai tích lũy
var_explained <- pca_wdbc$sdev^2 / sum(pca_wdbc$sdev^2)
cum_var <- cumsum(var_explained)
plot(cum_var, type = "b", pch = 19, col = "red",
main = "Biểu đồ tỷ lệ phương sai tích lũy",
xlab = "Số thành phần chính",
ylab = "Tỷ lệ tích lũy")
# Biplot cho 2 thành phần đầu tiên
biplot(pca_wdbc,
scale = 0,
cex = c(0.6, 0.8),
col = c("gray", "blue"),
main = "Biplot hai thành phần chính đầu tiên")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
scores <- data.frame(pca_wdbc$x)
plot(scores$PC1, scores$PC2,
col = ifelse(Diagnosis == 2, "red", "green"),
pch = 19,
xlab = "PC1",
ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
pca_wdbc <- prcomp(data_scale, cor = T)
pca_wdbc <- prcomp(data_scale, scale = T)
pca_wdbc <- princomp(data_scale, cor = T)
summary(pca_wdbc)
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
plot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)",
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai")
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
screeplot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)",
xlab = "Thành phần chính",
ylab = "Tỷ lệ phương sai")
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
screeplot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)",
ylab = "Tỷ lệ phương sai")
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
screeplot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)")
# Biểu đồ tỷ lệ phương sai tích lũy
var_explained <- pca_wdbc$sdev^2 / sum(pca_wdbc$sdev^2)
cum_var <- cumsum(var_explained)
plot(cum_var, type = "b", pch = 19, col = "red",
main = "Biểu đồ tỷ lệ phương sai tích lũy",
xlab = "Số thành phần chính",
ylab = "Tỷ lệ tích lũy")
# Biplot cho 2 thành phần đầu tiên
biplot(pca_wdbc,
scale = 0,
cex = c(0.6, 0.8),
col = c("gray", "blue"),
main = "Biplot hai thành phần chính đầu tiên")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
scores <- data.frame(pca_wdbc$x)
plot(scores$PC1, scores$PC2,
col = ifelse(Diagnosis == 2, "red", "green"),
pch = 19,
xlab = "PC1",
ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
scores <- data.frame(pca_wdbc$x)
plot(scores$PC1, scores$PC2,
col = ifelse(diag == 2, "red", "green"),
pch = 19,
xlab = "PC1",
ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
scores <- data.frame(pca_wdbc$x)
scores
head(scores)
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
pca_wdbc = prcomp(data_scaled, center = TRUE, scale. = TRUE)
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
pca_wdbc = prcomp(data_scale, center = TRUE, scale. = TRUE)
scores = data.frame(pca_wdbc$x[, 1:2], diag)
plot(scores$PC1, scores$PC2,
col = ifelse(scores$Diagnosis == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
legend("topright",
legend = c("Benign", "Malignant"),
col = c("green", "red"),
pch = 19)
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
pca_wdbc = prcomp(data_scale, center = TRUE, scale. = TRUE)
scores = data.frame(pca_wdbc$x[, 1:2], diag)
plot(scores$PC1, scores$PC2,
col = ifelse(scores$diag == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
legend("topright",
legend = c("Benign", "Malignant"),
col = c("green", "red"),
pch = 19)
scores = data.frame(pca_wdbc$x[, 1:2],
Diagnosis = wdbc$diagnosis)  # giữ nguyên nhãn 'B' và 'M'
# 3️⃣ Vẽ biểu đồ phân tán
plot(scores$PC1, scores$PC2,
col = ifelse(scores$Diagnosis == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
pca_wdbc = prcomp(data_scale, center = TRUE, scale. = TRUE)
scores = data.frame(pca_wdbc$x[, 1:2],
Diagnosis = wdbc$diagnosis)  # giữ nguyên nhãn 'B' và 'M'
# 3️⃣ Vẽ biểu đồ phân tán
plot(scores$PC1, scores$PC2,
col = ifelse(scores$Diagnosis == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
legend("topright",
legend = c("Benign", "Malignant"),
col = c("green", "red"),
pch = 19)
plot(scores$PC1, scores$PC2,
col = ifelse(scores$Diagnosis == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
legend("topright",
legend = c("Benign", "Malignant"),
col = c("green", "red"),
pch = 19)
# Bài 2:
# Bộdữliệu wdbc {kdevine}trongRđượcthuthậptừBệnhviệnĐạihọcWisconsin,Madison, và được sử dụng để hỗ trợ trong việc chẩn đoán ung thư vú thông qua các đặc điểm của tế bào. Bộ dữ liệu gồm 569 quan sát và 31 biến, cụ thể các đặc trưng: Radius (Độdài trung bình từ tâm đến các điểm trên chu vi), Texture (Độ lệch chuẩn của các giá trịxám), Perimeter (Chu vi của tế bào), Area (Diện tích của tế bào), Smoothness (Biến thiêncục bộ trong độ dài bán kính), Compactness (Tính toán từ chu vi và diện tích), Concavity(Mức độ nghiêm trọng của các phần lõm trên đường viền), Concave Points (Số lượng phầnlõm trên đường viền), Symmetry (Độ đối xứng của tế bào), Fractal Dimension (Đánh giá độphức tạp của hình dạng tế bào)
# install.packages("kdevine")
library(kdevine)
data(wdbc)
#1.  Tiến hành tiền xử lý dữ liệu, bao gồm việc loại bỏ các cột không cần thiết và chuyển đổi các biến phân loại thành biến số.
# loại bỏ các cột không cần thiết (id, diagnosis)
# Giữ lại các cột đặc trung
data = wdbc[, -c(1, 2)]
diag = as.numeric(as.factor(wdbc$diagnosis))
# 2. Chuẩn hóa dữ liệu để đảm bảo rằng tất cả các biến có cùng quy mô:
data_scale = scale(data)
data_scale
# 3.  Tính toán ma trận hiệp phương sai và tìm giá trị riêng, véc-tơ riêng.
cov_matrix = cov(data_scale)
eig = eigen(cov_matrix)
eig$values
eig$vectors
# 4. Thực hiện PCA và phân tích kết quả. Sau đó, vẽ các biểu đồ liên quan đến PCA, bao  gồm biplot, biểu đồ tỷ lệ phương sai giải thích và biểu đồ phân tán các thành phần chính
pca_wdbc <- princomp(data_scale, cor = T)
summary(pca_wdbc)
# Biểu đồ tỷ lệ phương sai giải thích (Scree plot)
screeplot(pca_wdbc,
type = "l",
col = "blue",
main = "Biểu đồ tỷ lệ phương sai của các thành phần chính (Scree Plot)")
# Biểu đồ tỷ lệ phương sai tích lũy
var_explained <- pca_wdbc$sdev^2 / sum(pca_wdbc$sdev^2)
cum_var <- cumsum(var_explained)
plot(cum_var, type = "b", pch = 19, col = "red",
main = "Biểu đồ tỷ lệ phương sai tích lũy",
xlab = "Số thành phần chính",
ylab = "Tỷ lệ tích lũy")
# Biplot cho 2 thành phần đầu tiên
biplot(pca_wdbc,
scale = 0,
cex = c(0.6, 0.8),
col = c("gray", "blue"),
main = "Biplot hai thành phần chính đầu tiên")
# Biểu đồ phân tán (scatter plot) theo PC1 và PC2
pca_wdbc = prcomp(data_scale, center = TRUE, scale. = TRUE)
scores = data.frame(pca_wdbc$x[, 1:2],
Diagnosis = wdbc$diagnosis)  # giữ nguyên nhãn 'B' và 'M'
# 3️⃣ Vẽ biểu đồ phân tán
plot(scores$PC1, scores$PC2,
col = ifelse(scores$Diagnosis == "M", "red", "green"),
pch = 19,
xlab = "PC1", ylab = "PC2",
main = "Biểu đồ phân tán PC1 và PC2 (Màu theo Diagnosis)")
legend("topright",
legend = c("Benign", "Malignant"),
col = c("green", "red"),
pch = 19)
#5.   Viết nhận xét về kết quả phân tích và ý nghĩa của các thành phần chính
