y[c(1,3,5)]
x[-c(4,5,6)]
x = c(1:9)
y = c(1.5, 2.3 ,3.2 , 4.6, 5.4, 6.6, 7.6, 8.6, 9.1)
x
y
print(paste("Độ dài vector x là: ", length(x)))
cat("Độ dài vector y là: ", length(y), "\n")
cat("tổng của 2 vector: ", x + y)
cat("hiệu của 2 vector: ", x - y)
cat("tích của 2 vector: ", x * y)
cat("chuẩn của x: ", sqrt(sum(x^2)))
cat("Chuẩn của y: ", sqrt(sum(y^2)))
y[c(1,3,5)]
x[-c(4,5,6)]
?prod
print(prod(1:7))
x
a = prod(x[-length(x)])
x[length(x)] = a
x
y[2] = 99
y[7] = 199
y
knitr::opts_chunk$set(echo = TRUE)
#(1) Tìm hệ số tương quan mẫu giữa Sepal.Length và Sepal.Width; Petal.Length và Petal.Width.
cor(iris$Sepal.Length, iris$Sepal.Width)
cor(iris$Petal.Length, iris$Petal.Width)
#(2) Kiểm định sự tương quan giữa Sepal.Length và Sepal.Width; Petal.Length và Petal.Width.
cor.test(iris$Sepal.Length, iris$Sepal.Width)
#(3) Viết mô hình hồi quy tuyến tính của Sepal.Width theo Petal.Width. Các hệ số trong
#mô hình hồi quy có thực sự khác 0 không? Phần dư trong mô hình có tuân theo phân
#phối chuẩn với giá trị trung bình bằng 0 không?
lmfit = lm(iris$Sepal.Width ~ iris$Petal.Width)
summary(lmfit)
a0_h <- lmfit$coefficients[1]
lmfit <- lm(train$dist ~ train$speed)
a0_h <- lmfit$coefficients[1]
a1_h <- lmfit$coefficients[2]
y_h <- a0_h + a1_h * x
#(1) Sử dụng câu lệnh set.seed(3) và chia bộ dữ liệu cars thành hai bộ dữ liệu con: training
#(65%) và testing (35%).
sample <- sample(c(TRUE, FALSE), nrow(cars), replace=TRUE, prob=c(0.65,0.35))
sample
table(sample)
train <- cars[sample, ]
View(train)
#a. Tìm giá trị trung bình và độ lệch tiêu chuẩn của độ dài đường đua,
#độ cao đạt được và thời gian chạy của người chiến thắng
?colMeans
print("Giá trị trung bình")
colMeans(hills)
colMeans(hills)
# Độ biến thiên tổng thể của dữ liệu thật y, nếu dữ liệu phân tán nhiều thì TSS lớn
TSS <- sum((y - mean(y))^2)
# Độ biến thiên tổng thể của dữ liệu thật y, nếu dữ liệu phân tán nhiều thì TSS lớn
(TSS <- sum((y - mean(y))^2))
# install.packages("datasets")
# library(datasets)
# View(iris)
data <- iris[, 1:4] #Chỉ lấy các cột chứa dữ liệ về độ dài, rộng của lá, đài hoa
print("Giá trị trung bình:")
print(sapply(data, mean))
print("Độ lệch tiêu chuẩn:")
print(sapply(data, sd))
attach(data)
z1 <- Petal.Length # ĐỘ dài cánh hoa
z2 <- Petal.Width # Độ rộng cánh hoa
z3 <- Sepal.Length # Độ dài đài hoa
z4 <- Sepal.Width # Độ rộng đài hoa
# Bài toán:
# H0: Zi tuân theo pp chuẩn ,i = 1->4
# H1: Zi không tuân theo pp chuẩn
print("Kiểm định phân phối chuẩn cho độ dài của cánh hoa:")
shapiro.test(z1)
# Do p-value = 7.412e-10 < 0.05 nên bác bỏ H0,
# KL: Với mức ý nghĩa 5%, có cơ sở để nói biến độ dài cánh hoa không tuân theo phân phối chuẩn
print("Kiểm định phân phối chuẩn cho độ rộng của cánh hoa:")
shapiro.test(z2)
# Do p-value = 1.68e-08 < 0.05 nên bác bỏ H0,
# KL: Với mức ý nghĩa 5%, có cơ sở để nói biến độ rộng cánh hoa không tuân theo phân phối chuẩn
print("Kiểm định phân phối chuẩn cho độ dài của đài hoa:")
shapiro.test(z3)
# Do p-value = 0.01018 < 0.05 nên bác bỏ H0,
# KL: Với mức ý nghĩa 5%, có cơ sở để nói biến độ dài đài hoa không tuân theo phân phối chuẩn
print("Kiểm định phân phối chuẩn cho độ rộng của đài hoa:")
shapiro.test(z4)
# Do p-value = 0.1012 > 0.05 nên không bác bỏ H0,
# KL: Với mức ý nghĩa 5%, có cơ sở để nói biến độ rộng đài hoa tuân theo phân phối chuẩn
par(mfrow = c(1,2))
hist(z4, main = "Hàm mật độ của độ rộng đài hoa", xlab = "Độ rộng đài hoa (cm)", ylab = "Mật độ", cex.main = 0.8)
plot(ecdf(z4), main = "Hàm phân phối của độ rộng đài hoa", xlab = "Độ rộng đài hoa (cm)", ylab = "Xác suất", cex.main = 0.8)
# Bài 4:
# Gọi x là khoảng cách của oto
x = na.omit(cars$dist)
# Bài 4:
# Gọi x là khoảng cách của oto
x = na.omit(cars$dist)
length(x)
```{r}
# Bài 4:
# Gọi x là khoảng cách của oto
x = na.omit(cars$dist)
length(x)
# Kiểm tra dist tuân theo phân phối chuẩn,  n > 30 và n <= 50,  shapiro.test(d) kiểm tra xem nó có chuẩn không
?shapiro.test()
shapiro.test(x)
#Do p-value = 0.0391 < 0.05 nên bác bỏ giả thuyết dist tuân theo phân phối chuẩn
# Do không tuân theo phân phối chuẩn -> willcolson
wilcox.test(x, mu = 45, alternative = "greater", conf.level = 0.90)
# b. Với mức ý nghĩa 2%, kiểm định xem tỷ lệ số xe có tốc độ- “speed” nhỏ hơn 13 trên
# tổng số xe có lớn hơn 0.4 không? Giải thích các kết quả thu được
(n = length(cars$speed))
#Do p-value = 0.0391 < 0.05 nên bác bỏ giả thuyết dist tuân theo phân phối chuẩn
# Do không tuân theo phân phối chuẩn -> willcolson
wilcox.test(x, mu = 45, alternative = "greater", conf.level = 0.90)
